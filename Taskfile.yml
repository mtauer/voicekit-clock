version: "3"

dotenv: [".env"] # Load environment variables from .env

tasks:
  sync:
    desc: Sync code to Raspberry Pi
    cmds:
      - rsync -avz ./src/voicekit-clock/ $PI_HOST:$PI_PROJECT_PATH --exclude .env
